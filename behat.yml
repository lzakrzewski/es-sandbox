default:
    autoload:
        - "%paths.base%"
    suites:
        default:
            filters:
                tags: "~@incomplete"
            contexts:
                - tests\contexts\BasketContext:

    extensions:
        Behat\Symfony2Extension:
            kernel:
                class: "MicroKernel"